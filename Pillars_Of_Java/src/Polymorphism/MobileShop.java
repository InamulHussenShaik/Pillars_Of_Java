package Polymorphism;

public class MobileShop {
	//polymorphism achivement
	void getReferenceType(Mobile ref)
	{
		ref.calls();
		ref.chat();
		ref.capture();
		ref.gps();
		ref.transactions();
	}
}
